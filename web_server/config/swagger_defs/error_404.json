{
    "type": "object",
    "properties": {
        "message": {
            "type" : "string",
            "description": "Results not found.",
            "example": "Results not found."
        }
    }
}
